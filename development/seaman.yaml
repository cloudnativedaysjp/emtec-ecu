debug: false
slack:
  botToken: ${SLACK_BOT_TOKEN}
  appToken: ${SLACK_APP_TOKEN}
github:
  username: ShotaKitazawa
  accessToken: ${GITHUB_ACCESS_TOKEN}
release:
  targets: []
broadcast:
  endpointUrl: ${BROADCAST_ENDPOINT_URL}

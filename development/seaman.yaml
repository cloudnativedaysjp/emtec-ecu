debug: false
slack:
  botToken: ${SLACK_BOT_TOKEN}
  appToken: ${SLACK_APP_TOKEN}
github:
  username: ShotaKitazawa
  accessToken: dummy
release:
  targets: []
emtec:
  endpointUrl: ${EMTEC_ECU_ENDPOINT_URL}
  dreamkast:
    endpointUrl: ${DK_ENDPOINT_URL}
    auth0Domain: ${DK_AUTH0_DOMAIN}
    auth0ClientId: ${DK_AUTH0_CLIENT_ID}
    auth0ClientSecret: ${DK_AUTH0_CLIENT_SECRET}

